# RESTful-API
A simple RESTful API built with Node.js and Express.js following a modular architecture using routes, controllers, middleware, and utils. The project demonstrates CRUD operations for resources like products, users, cart, and orders, along with request validation and server-side error handling.
